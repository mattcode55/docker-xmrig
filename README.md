# docker-xmrig

## Example

    docker run --rm -it asymptotically/xmrig:latest \
      -o localhost:3333 \
      -u 44LXLhWVnFmXhvHku... \
      -p mydockerworker \
      --threads 8 \
      --av 1 \
      --donate-level 1
